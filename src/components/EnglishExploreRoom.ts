export type EnglishExploreRoomProps = {
  today: string;
  energyMode: "low" | "normal" | "high";
};

export const EnglishExploreRoom = "Runtime implementation lives in public/modules/EnglishExploreRoom.js for the current no-build prototype.";
